defaults: &defaults
  adapter: mysql
  reconnect: false
  username: root
  password:
  host: localhost
  pool: 5
  encoding: utf8

development:
  <<: *defaults
  adapter: mysql2
  database: statturk_dev 


#sudo service mysqld start